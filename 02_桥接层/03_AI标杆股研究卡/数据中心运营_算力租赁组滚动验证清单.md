# 数据中心运营_算力租赁组滚动验证清单

> 所属模块：02_桥接层/03_AI标杆股研究卡
> 生成日期：2026-06-29
> 使用目的：把数据中心运营_算力租赁组后续该盯的 `上架率 / 利用率 / 租赁价格 / AIDC收入占比 / OCF / 折旧 / 融资结构` 压成统一执行清单。
> 使用边界：承接现有正式披露与现有组内分层，不新增传闻层结论。

---

## 一、滚动验证总表

| 标的 | 当前定位 | 最关键下一条验证数据 | 加分信号 | 减分 / 证伪信号 |
|---|---|---|---|---|
| 润泽科技 | `AIDC业绩释放位` | AIDC收入占比、上架率、利用率与 OCF 持续性 | 交付继续放量、AIDC收入占比提升、OCF 延续强势 | 长期应付款继续抬升但 OCF 转弱，资本循环开始吃紧 |
| 数据港 | `稳态IDC现金流 + 智算导入位` | 智算相关收入、上架率 / 利用率与 OCF 延续性 | 稳态现金流维持、智算导入提速、利润与 OCF 同步改善 | 智算导入长期停留在口径层，折旧与增长弹性失衡 |
| 奥飞数据 | `区域/扩张型IDC平台位` | IDC 与算力服务收入、OCF、借款与长期应付款变化 | 收入与 OCF 同步改善、在建工程转固后回报稳定 | 资产扩张继续放大但 OCF 弱化 |
| 光环新网 | `云/IDC混合平台转型位` | 上架率、租赁价格、IDC 毛利率、OCF | 上架率提升、租赁价格企稳、回款改善 | 上架率停留低位、租赁价格继续下行、OCF 继续下滑 |
| 宝信软件 | `高质量园区/IDC平台补位` | 宝之云项目交付、算力服务口径、OCF 持续性 | 项目持续交付、专项贷款受控、OCF 保持强势 | 项目继续投入但分部回报率长期不清晰 |
| 首都在线 | `高弹性租赁/调度叙事 + 高波动风险位` | 智算云收入占比、OCF、在建工程、借款/融资租赁变化 | 智算收入占比提升且 OCF 修复 | OCF 再次走弱、借款/诉讼/融资租赁压力继续放大 |

---

## 二、分标的执行要点

### 润泽科技

1. 优先盯中报、三季报中的 AIDC 收入占比与交付节奏。
2. 看 OCF 是否继续和收入扩张同步，而不是只剩利润表强。
3. 看长期应付款、融资结构和折旧压力是否开始快于经营兑现上升。

### 数据港

1. 优先盯成熟 IDC 托管业务的稳态 OCF 是否保持。
2. 看智算业务导入是否从“布局”升级到更明确的收入和客户口径。
3. 看利用率、上架率和折旧是否共同支撑转型效率，而不是只有概念升级。

### 奥飞数据

1. 优先盯 IDC 与算力服务收入能否继续带动 OCF。
2. 看固定资产、长期借款和长期应付款扩张是否快于经营兑现。
3. 看区域平台扩张能否继续转成更稳的客户结构和现金流。

### 光环新网

1. 优先盯上架率、租赁价格和 IDC 毛利率。
2. 看云业务承压是否继续拖累整体平台经营质量。
3. 看回款与 OCF 是否修复，否则大投放逻辑会被快速审计。

### 宝信软件

1. 优先盯宝之云项目持续交付和资源运营口径是否更细。
2. 看在建工程向固定资产转化后的回报率。
3. 看 OCF 是否继续强于利润波动。

### 首都在线

1. 优先盯智算云收入占比能否继续抬升。
2. 看 OCF 能否从 Q1 转负重新修复。
3. 看借款、融资租赁、在建工程和诉讼风险是否继续恶化。

---

## 三、组级观察原则

### 判断

1. 本组核心不是“算力租赁热度”，而是“资源平台经营质量”。
2. `润泽科技` 更适合按 `交付 -> 收入结构 -> OCF -> 融资与折旧审计` 路径跟踪。
3. `数据港` 更适合按 `稳态现金流 -> 智算导入 -> 转型效率` 路径跟踪。
4. `奥飞数据` 更适合按 `区域扩张 -> 收入与 OCF -> 资产负债表审计` 路径跟踪。
5. `光环新网` 更适合按 `机柜投放 -> 上架率 / 租赁价格 -> OCF 与毛利率审计` 路径跟踪。
6. `宝信软件` 更适合按 `项目交付 -> 园区运营 -> 回报率审计` 路径跟踪。
7. `首都在线` 更适合按 `智算收入占比 -> OCF -> 折旧 / 借款 / 诉讼审计` 路径跟踪。
8. 资源运营层必须把机柜、交付、现金流、折旧和负债放到同一张表里看，不能只看收入增速。

---

## 四、当前组级动作建议

1. `第一优先`：继续跟 `润泽科技` 的 AIDC 收入占比、交付节奏和 OCF。
2. `第二优先`：继续跟 `光环新网` 的上架率、租赁价格和 OCF。
3. `第三优先`：继续跟 `数据港` 与 `奥飞数据` 的现金流与智算导入效率。
4. `第四优先`：把 `宝信软件` 与 `首都在线` 作为质量补位和风险补位滚动维护。
5. `长期动作`：统一补这组最缺的连续口径：
   - 上架率
   - 利用率
   - GPU / 算力租赁价格
   - 长期应付款与折旧压力

---

## 五、截至 2026-07-02 的中报前核验补记

1. 已完成本组第一轮中报前正式核验，新增正式披露主要集中在 `光环新网`、`奥飞数据` 与 `宝信软件`。
2. `光环新网`
   - 已新增核验到 `上架率约60%`、已签约及达成意向机柜比例约 `90%`、算力业务规模超过 `4,000P` 的 IR 口径。
   - 当前更应继续盯 `上架率 -> 租赁价格 -> OCF`，而不是只看资源规模。
3. `奥飞数据`
   - 已新增核验到 `2026年` 多地交付计划与持有型 ABS 受理。
   - 当前更应继续盯 `交付 -> 收入 -> OCF -> ABS 是否落地`。
4. `宝信软件`
   - 已新增核验到继续推进罗泾、华北基地智算业务的正式行动方案口径。
   - 当前更应继续盯 `项目交付 -> 转固 -> OCF -> 回报率`。
5. 截至当前，本组 6 个样本暂未补到明确的 `2026H1` 业绩预告，报表硬口径仍以 `2025年报 + 2026Q1` 为主。

## 六、2026-07-15 外围反证样本：科华数据

- `科华数据`：H1 预计归母净利润 `3.65-3.90 亿元`，但扣非仅同比 `+2.79%-13.68%`；归母增长含出售算力相关子公司 `80%`股权的非经常性收益。
- 审计处理：不把该业绩预告计入 IDC/AIDC 运营或算力租赁的经营验证。公告主要归因于海外市场拓展，未给出可比的数据中心收入、上架率、利用率、租赁价格、回款或 OCF。
- 组级含义：该反证强化“利润标题不能替代资源运营质量”的规则。IDC/AIDC 维持以 `上架率/利用率 -> 分部收入 -> OCF -> 融资与折旧` 的闭环为上调条件。
- 来源：[科华数据 2026 年半年度业绩预告（巨潮资讯）](https://static.cninfo.com.cn/finalpage/2026-07-14/1225422103.PDF)。

## 七、2026-07-15 外围算力样本补充

- `利通电子`：正式业绩预告披露 2026H1 归母净利润 `6.50-7.50 亿元`、扣非归母 `5.55-6.68 亿元`，公告明确将“算力业务收入持续增长、算力经销业务增长显著”列为预增主因。
- 样本角色：`外围经营验证样本`，不并入本组 IDC/AIDC 运营核心样本。其业务口径是算力经销，尚未给出可比的上架率、利用率、租赁价格、OCF 或资本开支拆分。
- 传导：`算力业务收入增长 -> 利润增长` 已获正式披露支持；`利润增长 -> 资源运营组级改善` 尚不成立。
- 下一验证：2026H1 定期报告中的算力业务收入占比、客户/服务期、应收账款与 OCF。若这些字段不能闭环，保留为单公司旁证，不上收为 IDC/AIDC 主线确认。
- 来源：巨潮资讯，2026-07-15《江苏利通电子股份有限公司 2026 年半年度业绩预增公告》；Wiki 关联卡 [[利通电子]]。
