# AI算力基建大组扩展观察池

> 所属模块：02_桥接层/02_AI产业链研究卡
> 生成日期：2026-07-04
> 研究定位：把窄口径 `AIDC供配电` 三路径样本，扩展为 `AI算力基建大组` 的统一观察池，用于判断资金和经营验证是否从核心供配电扩散到液冷温控、IDC / AIDC 运营和电力设备旁证层。
> 使用边界：本文是桥接层观察池，不是买入清单；扩大样本范围不等于扩大结论确定性，低 AI 纯度样本只能作为旁证，不能直接替代核心供配电样本。

---

## 一句话结论

`科华数据 / 金盘科技 / 科士达` 三只样本适合做 `AIDC供配电` 的核心三路径锚，但样本太小，不能单独代表完整 AI 算力基建板块。后续应把观察范围扩成：

`核心供配电 -> 电力设备旁证 -> 液冷 / 温控 -> IDC / AIDC运营`

其中，窄组三只继续负责选股和经营闭环验证；大组观察池负责判断是否真的出现 `AI算力基建` 组级共振。

---

## 一、为什么要扩组

### 事实

1. 结果层 `AIDC供配电三路径接力判定表.md` 已经把 `科华数据 / 金盘科技 / 科士达` 拆成三条路径：
   - `订单型高弹性设备`
   - `平台型智算供电基础设施`
   - `标准化稳态兑现`
2. 但三只样本只能回答 `供配电内部谁先拿到订单 / 收入 / OCF 闭环`，不能回答完整基建链是否启动。
3. 现有库中已经有 `液冷_散热二线弹性组`、`数据中心运营_算力租赁组` 和 `AI产业链候选标的池.md`，说明基础设施协同层本来就不只一组样本。
4. 最新周更口径仍显示：`AIDC供配电` 未确认接力，`液冷` 未确认组级扩散，`IDC / AIDC运营` 仍是修复观察。

### 判断

扩组的目的不是降低门槛，而是提高判断质量：

1. 如果只有三只供配电样本转强，可能只是小组内补涨；
2. 如果供配电、液冷、IDC运营、电力设备旁证同时改善，才更像 AI 算力基建大组共振；
3. 如果大组扩散但经营层没有 `收入 / 利润 / OCF / 回款` 验证，则更可能是题材外溢，而不是接力确认。

---

## 二、扩展观察池

| 层级 | 样本 | 第一变量 | 核心用途 | 使用边界 |
|---|---|---|---|---|
| 核心供配电组 | `科华数据`、`金盘科技`、`科士达` | 数据中心收入、订单、OCF、AI纯度 | 选股和三路径经营闭环验证 | 核心主样本，仍按三路径表逐季审计 |
| 电力设备旁证组 | `思源电气`、`许继电气`、`特变电工`、`中国西电`、`平高电气` | 变压器、开关、电网设备订单与海外交付 | 判断电力设备是否承接 AIDC 电力需求 | AI纯度通常较低，只能做旁证，不可直接上调为 AIDC 主线 |
| 液冷 / 温控组 | `英维克`、`申菱环境`、`高澜股份`、`飞荣达`、`佳力图`、`曙光数创` | 液冷订单、收入确认、毛利率、回款、OCF | 判断高功率机柜散热需求是否扩散 | 液冷二线波动大，必须区分订单、收入和现金流 |
| IDC / AIDC运营组 | `润泽科技`、`奥飞数据`、`数据港`、`光环新网`、`宝信软件`、`首都在线` | 上架率、利用率、租赁价格、折旧、融资、OCF | 验证下游数据中心需求和资源运营质量 | 重资产属性强，不能只看算力叙事 |

---

## 三、窄组和大组的分工

### 1. 窄组：用于选股和经营闭环

窄组仍然是：

`科华数据 / 金盘科技 / 科士达`

它解决三个问题：

1. `金盘科技` 的 `订单 -> 收入 -> OCF` 能否闭环；
2. `科华数据` 的 `分部收入 -> 交付效率 -> OCF` 能否继续强化；
3. `科士达` 的 `数据中心核心业务 -> AI纯度 -> OCF持续性` 能否进一步清晰。

### 2. 大组：用于判断是否成为基建共振

大组观察池解决四个问题：

1. 供配电是否不再只是三只个股的局部修复；
2. 液冷 / 温控是否从单点强势扩散到组级行情；
3. IDC / AIDC运营是否从退潮修复变成真实运营改善；
4. 电力设备旁证是否出现与 AIDC 电力需求相关的订单、交付或收入拆分。

---

## 四、组级升级标准

### 1. 观察层

默认当前状态仍应写为：

`经营有线索，行情未确认，扩组后仍是观察池。`

触发条件：

1. 供配电三只样本尚未形成 `20日转正 + 60日超过30%`；
2. 液冷仍偏单点强势；
3. IDC / AIDC运营仍缺上架率、利用率和 OCF 连续改善。

### 2. 初步修复层

可以上调为 `基建修复观察` 的条件：

1. 核心供配电组至少两只 `20日涨幅转正`；
2. 液冷 / 温控组不再只有单点强势，至少两只同步改善；
3. IDC / AIDC运营层至少出现一只经营层强验证样本；
4. 新增公告或中报至少补出 `订单 / 收入 / OCF` 中的一项正式升级。

### 3. 接力候选层

可以上调为 `基建接力候选` 的条件：

1. 核心供配电组 `60日组均涨幅` 接近或超过 `30%`；
2. 大组至少三个子层中有两个子层同步转强；
3. 至少一条路径形成 `订单 / 收入 / OCF` 连续闭环；
4. 另外一条路径不拖后腿，证明不是孤立个股行情。

### 4. 主线确认层

只有同时满足行情和经营两类条件，才可写成 `AI算力基建主线确认`：

1. 行情层：供配电、液冷、IDC运营或电力设备旁证中，至少三个子层出现组级强度；
2. 经营层：核心供配电或液冷 / 温控里至少两家公司出现收入、利润、OCF 同步改善；
3. 证伪层：应收、存货、合同负债、在建工程、融资成本没有明显恶化；
4. 资金层：不只是题材换手上升，而是机构持仓代理或成交结构同步改善。

---

## 五、变量表

| 变量 | 频率 | 对应层级 | 当前用法 | 加分条件 | 减分条件 |
|---|---|---|---|---|---|
| 数据中心订单 | 季度 / 公告 | 核心供配电、电力设备旁证 | 验证需求是否真实进入设备链 | 订单继续高增且客户 / 项目口径清楚 | 只重复订单口径，收入不跟 |
| 数据中心收入拆分 | 季度 / 年报 | 核心供配电、IDC运营 | 验证订单是否转收入 | 分部收入继续增长，AI纯度更清楚 | 收入口径模糊或被新能源 / 传统业务掩盖 |
| OCF / 回款 | 季度 | 全部层级 | 判断利润质量 | OCF与利润同步改善 | 利润增长但 OCF 持续走弱 |
| 上架率 / 利用率 | 季度 / 年报 | IDC / AIDC运营 | 判断资源运营质量 | 上架率和利用率提升，租赁价格稳定 | 折旧和融资压力先于收入改善 |
| 液冷订单与毛利率 | 季度 / 公告 | 液冷 / 温控 | 判断散热需求是否扩散 | 订单转收入，毛利率改善，回款同步 | 订单强但收入确认和现金流错位 |
| 应收 / 存货 / 合同负债 | 季度 | 项目制设备与液冷 | 判断项目制风险 | 合同负债抬升伴随收入和回款改善 | 备货重、应收升、OCF不改善 |
| 20日 / 60日组均涨幅 | 周度 | 全部层级 | 判断行情是否共振 | 20日转正，60日接近或超过30% | 只有单只强，组均仍弱 |
| 成交额 / 换手 | 周度 | 全部层级 | 判断资金参与 | 成交放大但未极端拥挤 | 高换手无经营增量 |

---

## 六、当前正式口径

截至本版，扩组后的正式口径应写为：

1. `AIDC供配电` 三只窄组仍是核心经营锚；
2. `AI算力基建大组` 已补成观察池，但尚未形成主线确认；
3. `液冷 / 温控`、`IDC / AIDC运营` 与 `电力设备旁证` 只能用于判断共振，不应直接替代三路径经营闭环；
4. 后续若大组走强，必须回到 `订单 / 收入 / OCF / 回款 / 利用率 / AI纯度` 做证据审计。

---

## 七、回写顺序

后续如果新增正式公告、中报或周度行情变化，回写顺序固定为：

1. 本文：先判断新增样本落在哪个子层；
2. `AI供配电与智算电力基础设施滚动评分表.md`：更新核心供配电三只；
3. `AI资源运营与液冷二线经营质量滚动评分表.md`：更新液冷与资源运营层；
4. `AIDC供配电三路径接力判定表.md`：只更新窄组三路径接力判断；
5. `AI算力行情演绎周更看板.md` 和 dated 周更：更新大组行情强度；
6. `AI全链条估值_预期_拥挤审计总表.md`：把估值、拥挤和赔率统一回写；
7. `项目状态面板.md`：只有阶段口径变化时再回写。

---

## 八、下一步最优先补证

1. 给大组观察池补一张周度行情表：`5日 / 20日 / 60日 / YTD / 距60日高点 / 组内上涨家数`。
2. 对电力设备旁证组逐只确认 `AIDC / 数据中心 / 海外电力设备` 收入口径，不能只用电网设备总增长替代。
3. 对液冷 / 温控组补 `液冷订单占比 / 收入确认 / 毛利率 / OCF`。
4. 对 IDC / AIDC运营组补 `上架率 / 利用率 / 租赁价格 / 折旧 / 融资成本`。
5. 若后续周更继续显示大组不共振，则维持 `观察池`，不得升级为 `基建接力`。

---

## 九、2026-07-05 商业研报新增线索回写

### 事实

1. `Citi` 近 `1-3` 日公开增量里，存在 `Overcoming Gridlock 2.0: Solving Power Bottlenecks to Drive AI, Energy Security` 与 `China's 2H outlook: AI supercycle cuts into the K-shaped economy` 两条线索。
2. 前者把 `电网瓶颈 / 输电审批 / 发电侧投资 / 数据中心负荷增长` 作为 AI 基础设施的核心约束；后者把 `AI资本开支` 放在中国下半年结构分化框架里，与旧经济拖累并列讨论。
3. 这两条目前都只是 `公开摘要页 / 待核全文` 口径，尚不是仓库事实层。

### 判断

1. `AI算力基建大组` 后续不能只看 `供配电设备订单`，还要把 `电网瓶颈` 视为与 `机柜 / 液冷 / IDC运营` 并列的上游约束变量。
2. 海外这轮增量更强化 `AIDC供配电` 与 `电力设备旁证组` 的重要性，而不是直接强化 `IDC运营` 本身。
3. `中国AI超级周期` 只能作为宏观分化背景，暂时不能替代本组的 `订单 / 收入 / OCF / 上架率` 经营变量验证。

### 当前回写口径

1. `电力设备旁证组` 继续保留，但其观察优先级上调一档。
2. 后续补证顺序新增：`单柜功率 -> 并网周期 -> 电价 / 电力成本 -> 上架率 -> OCF`。
3. 若后续国内正式披露出现 `供配电 / 电网接入 / 电力成本` 明确口径，可再把这条线从海外观察层升级为组内正式变量。

---

## 十、2026-07-07 NVIDIA 800VDC专题回写

> 对应结果层专题报告：`03_结果层/01_总表整合与AI映射/NVIDIA_800VDC供电架构与A股映射评分_2026-07-07.md`。
> 本节用于桥接层落卡，不改变本文“观察池而非买入清单”的定位。

### 1. 新增变量

NVIDIA 800VDC把`AI算力基建大组`的第一变量从泛化的`电力瓶颈`进一步拆成：

`单柜功率密度 -> 转换级数 -> 铜耗/线缆体积 -> 800VDC直流配电 -> 机柜/节点侧高压DC转换 -> 订单/收入/OCF`

这意味着后续供配电研究不能只盯传统UPS或电网设备，还要把`机柜电源整机、800VDC/HVDC转换、功率器件、保护器件、SST`纳入同一观察池。

### 2. 本次样本角色扩展

| 层级 | 样本 | 本次角色 | 第一变量 | 使用边界 |
|---|---|---|---|---|
| NVIDIA生态A股承接锚 | `麦格米特` | Power system components直接映射 | NVIDIA官方生态列名、服务器/机柜电源产品、后续订单收入 | 列名不等于收入确认，仍需公告/IR回源 |
| 800VDC/HVDC技术承接锚 | `中恒电气` | 国内直流供电技术样本 | Panama/800V/HVDC产品、数据中心客户、项目交付 | 未见NVIDIA官方列名，不能越级成正式A |
| GPU电源经营验证样本 | `欧陆通` | 高功率数据中心电源样本 | GPU电源客户、数据中心电源收入、毛利率和OCF | 800VDC直接性待核 |
| 既有AIDC供配电三路径 | `金盘科技`、`科华数据`、`科士达` | 经营闭环样本 | 订单/收入/OCF/AI纯度 | 不因NVIDIA消息直接升级，只提高回源优先级 |
| SST / 中压直流旁证 | `中国西电`、`四方股份`、`伊戈尔` | 中长期路线旁证 | 13.8kVAC/800VDC、SST订单、保护控制 | 社区/研报转述需回源公司公告或公众号 |
| 功率器件/保护/被动件 | `斯达半导`、`天岳先进`、`三安光电`、`中熔电气`、`江海股份` | 二阶配套 | GaN/SiC、熔断、连接、电容 | 链条更长，不能压过电源整机 |

### 3. 本次阶段评分排序

> `2026-07-07`补充口径：新增`已公开业绩质量 / 未来业绩预期`为重要评分项。对应核验表为`03_结果层/01_总表整合与AI映射/NVIDIA_800VDC_A股样本业绩与预期核验表_2026-07-07.md`。
> 截至`2026-07-07`，Luna Stock MCP巨潮公告查询未命中下列样本`2026H1业绩预告`，因此未来业绩只能作为预期层，不得写成半年度业绩确认。

| 排名 | 样本 | 校准后阶段分 | 原分 | 业绩与预期影响 | 当前处理 |
|---:|---|---:|---:|---|---|
| 1 | 麦格米特 | 76 | 78 | 2026-2028 EPS预期弹性高，但2025扣非与OCF弱 | 重点跟踪，回踩承接后进攻候选 |
| 2 | 中恒电气 | 73 | 75 | Q1归母同比改善，但Q1 OCF转负、覆盖数少 | 强观察，等公司回源 |
| 3 | 欧陆通 | 68 | 72 | 2025经营较好，但2026Q1亏损 | GPU电源兑现候选，短期确定性下调 |
| 4 | 科士达 | 66 | 60 | 2025和2026Q1收入、利润、OCF较稳 | 稳态UPS/微模块审计，AI纯度待拆 |
| 5 | 金盘科技 | 65 | 64 | 订单和预期强，但Q1 OCF为负 | 持有审计，等周线修复与OCF改善 |
| 6 | 科华数据 | 64 | 61 | OCF和平台经营质量补分 | 平台型供配电经营审计 |
| 7 | 中熔电气 | 60 | 52 | 公开业绩质量强，保护器件方向优先回源 | 配套观察，不抢主线锚 |
| 8 | 四方股份 | 58 | 56 | SST/保护控制旁证，公开业绩相对稳但800VDC直接性待核 | 回源保护控制、直流配电和数据中心项目 |
| 9 | 中国西电 | 57 | 58 | SST中长期路线旁证，技术方向相关但兑现链条偏长 | 回源SST订单、交付、收入确认和800VDC适配 |

### 4. 对原观察池结论的影响

1. `AI算力基建大组`的重要性上调，但仍未升级为`主线确认`。
2. `核心供配电组三路径`仍按`订单 / 收入 / OCF / 未来业绩预期修正`确认接力，不被单条NVIDIA路线催化替代。
3. 新增`机柜电源整机`作为比传统UPS更靠近NVIDIA 800VDC的观察子层。
4. `SST`从长期泛电力设备旁证，升级为800VDC架构下需要重点回源的中长期路线，但当前仍是`C+/B-`阶段。
5. `中熔电气`因公开业绩质量强进入配套优先回源，但必须补AI数据中心直流保护订单，不能直接升成主线锚。

### 5. 后续回源顺序

1. `麦格米特`：NVIDIA列名后的公司公告、IR、产品、订单收入和利润率，重点验证未来EPS预期能否兑现。
2. `中恒电气`：800V/HVDC产品资料、客户、项目、收入和回款。
3. `欧陆通`：数据中心电源收入、GPU电源项目、800V产品路线，以及2026Q1亏损是否为一次性因素。
4. `科士达 / 科华数据 / 金盘科技`：继续按中报验证`收入 -> 利润 -> OCF -> 未来业绩预期修正`。
5. `中国西电`：西电电力电子SST订单原始来源、金额、交付和收入确认。
6. `中熔电气`：AI数据中心直流保护、熔断器或800VDC安全链条订单。
7. `Luna Stock MCP`：核心样本补`validate_cross_source`，使技术层具备正式A候选资格。
