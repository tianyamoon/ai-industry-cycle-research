# AI算力行情演绎周更｜模板

> 所属模块：03_结果层/01_总表整合与AI映射
> 周更日期：`YYYY-MM-DD`
> 对应最新交易日：`YYYY-MM-DD`
> 周更定位：作为 `AI算力行情演绎周更看板.md` 的滚动实填模板，记录当周主线、分化、接力和下周触发器。
> 使用边界：每周先更新 `AI算力行情演绎跟踪表.md` 与 `AI算力行情演绎周更看板.md`，再用本模板压缩成 dated 周更；若核心字段未更新，不应强写阶段迁移。
> 特别提示：若 `AIDC供配电` 仍是核心观察方向，正文必须判断是否需要拆成 `金盘科技 / 科华数据 / 科士达` 等内部路径；若讨论 `AI算力基建大组`，还必须区分 `核心供配电 / 电力设备旁证 / 液冷温控 / IDC-AIDC运营`，不能长期写成单一大类。
> 技术硬门槛：主动买入候选必须满足 `月线向上 + 周线向上`；日线只用于择时，不能替代月线和周线。

---

## 一句话结论

截至 `YYYY-MM-DD`，本轮 AI 算力行情处在：

`[硬件主线所处分段] + [基建接力是否确认] + [当前最弱方向]`

一句话正式表达：

> `[填写一句 40-80 字的阶段结论，不要写成笼统“AI算力仍强”；若基建方向重要，要说明其是否已拆成内部兑现路径。]`

---

## 一、本周最重要的变化

本周先回答三个问题：

1. `最强方向` 是否还在原来的细分里。
2. `高位方向` 是继续加速，还是已经切到分化审计。
3. `基建方向` 是否出现第一段明确接力，内部路径是否已经分化。
4. `新增买入候选` 是否同时满足月线和周线处于上升趋势。
5. `利率预期、存储成本和超大云厂 CapEx / FCF` 是否触发了高位成长与低位修复之间的风格再平衡。

当前最重要的三个事实：

1. `[细分A]` 的 `20日涨幅` 为 `[X%]`，相对 `[指数或上期值]` `[变强 / 变弱]`。
2. `[细分B]` 的 `距60日高点` 为 `[X%]`，说明其处在 `[高位加速 / 分化 / 修复]`。
3. `[细分C]` 的 `60日均涨幅` 为 `[X%]`，`20日均值` 为 `[X%]`，说明其 `[仍未确认接力 / 正在补涨 / 已进入主线讨论]`；若是 `AIDC供配电`，补一句内部先强的是 `[路径A / 路径B / 路径C]`。
4. `[新增候选]` 的 `月线趋势` 为 `[向上 / 走平 / 向下]`，`周线趋势` 为 `[向上 / 走平 / 向下]`，说明其 `[可进入主动买入候选 / 等待右侧 / 只能观察]`。
5. `[超大云厂样本]` 的 `CapEx / OCF / FCF / 组件涨价` 显示 `[扩建仍顺畅 / 已进入回报率审计]`；若 `HBM / 存储` 仍是核心方向，补一句其是否开始挤压 `[整机 / 机柜 / 柜外配套]` 预算。

---

## 二、本期记分牌

| 细分 | 当前阶段 | 月线趋势 | 周线趋势 | 主线强度 | 拥挤风险 | 接力概率 | 当前判断 |
|---|---|---|---|---:|---:|---:|---|
| HBM / 高性能存储代理 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| 先进封装 / 封测 / 材料 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| PCB / CCL / 高速互连辅材 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| 光模块 / 高速互联 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| 国产AI芯片 / 平台 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| AI服务器整机 / ODM | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| AIDC供配电 / UPS / 变压器 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| AI算力基建大组观察池 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| 液冷 / 温控 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |
| IDC / AIDC运营 / 算力租赁 | `[待填]` | `[向上/走平/向下]` | `[向上/走平/向下]` | `[1-5]` | `[1-5]` | `[1-5]` | `[一句话]` |

---

## 三、当前主线判断

### 1. 主线仍在什么地方

当前市场最愿意给溢价的，仍然是：

1. `[细分1]`
2. `[细分2]`
3. `[细分3]`

更准确的写法是：

1. `[高位加速方向]`
2. `[高位分化方向]`
3. `[底座兑现方向]`

### 2. 基建接力是否确认

`AIDC供配电 / 液冷 / IDC运营` 当前更接近：

1. `[低位观察 / 补涨加速 / 修复观察 / 退潮]`
2. 升级条件是 `[20日斜率 / 60日涨幅 / 订单 / OCF 等]`
3. 降级条件是 `[回撤 / 订单停滞 / 现金流走弱等]`
4. 若讨论 `资源运营 / 算力租赁`，必须补一句：`租赁收益率` 是否已经明确覆盖 `融资成本 + 折旧 + 运维`。

若 `AIDC供配电` 仍是核心观察方向，继续补一段：

1. `[路径A：订单型高弹性设备]`
2. `[路径B：平台型智算供电基础设施]`
3. `[路径C：标准化稳态兑现]`
4. `[本周哪条路径先强，验证变量是什么]`

若 `AI算力基建大组` 已经进入观察接力区，继续补一段：

1. `[核心供配电：三路径是否出现订单 / 收入 / OCF 闭环]`
2. `[电力设备旁证：是否能拆出 AIDC / 数据中心相关口径]`
3. `[液冷温控：是否从单点强势扩散到组级改善]`
4. `[IDC-AIDC运营：上架率 / 利用率 / OCF 是否同步修复]`

### 3. 当前不在主线里的方向

当前更适合放在修复观察层，而不是配置优先层的方向：

1. `[方向A]`
2. `[方向B]`

原因：

1. `[行情原因]`
2. `[基本面原因]`
3. `[资金风格原因]`

---

## 四、本期最该回答的三个问题

### 1. `[问题一标题]`

最需要盯的是：

1. `[指标1]`
2. `[指标2]`

如果 `[触发条件]`，则应把 `[细分]` 从 `[旧阶段]` 调整为 `[新阶段]`。

### 2. `[问题二标题]`

最需要盯的是：

1. `[指标1]`
2. `[指标2]`

如果 `[触发条件]`，说明 `[资金正在发生什么切换]`。

### 3. `[问题三标题]`

最需要盯的是：

1. `[指标1]`
2. `[指标2]`

只有当 `[月线向上] + [周线向上] + [行情条件] + [基本面条件]` 同时成立，才能把 `[细分]` 从观察位升级为接力位；若是 `AIDC供配电`，最好明确哪条内部路径先拿到这个资格。

---

## 五、下周最重要的动作

1. 更新 `[最强方向]` 的 `20日涨幅` 与 `距60日高点`。
2. 更新 `[老主线]` 龙头是否出现二次转强。
3. 更新 `[基建方向]` 的 `20日` 和 `60日` 斜率，看是否开始脱离低位观察。
4. 若 `AIDC供配电` 重要，补更新内部路径谁先出现 `订单 / 收入 / OCF` 共振。
5. 若 `AI算力基建大组` 重要，补更新四个子层的 `5日 / 20日 / 60日 / YTD / 距60日高点 / 组内上涨家数`。
6. 更新所有新增买入候选的 `月线趋势` 与 `周线趋势`。
7. 更新是否出现新的 `订单 / 收入 / 利润 / OCF` 验证。
8. 更新 `HBM / DRAM / NAND`、组件涨价和云厂 `CapEx / FCF` 是否出现新的正式口径。
9. 若涉及 `GB300 / 整柜 / 柜外配套`，检查是否有正式资料支持 `配置下修 / 砍单 / 预算挤压`，没有就明确写 `待核`。
10. 若阶段未变，只更新数字，不强改标签。

---

## 六、当前不该做的误判

1. 不要把 `[高位最强方向]` 的强势，误判成“现在最安全”。
2. 不要把 `[低位方向]` 的低位，误判成“已经确认接力”。
3. 不要把 `[长期空间方向]` 的长期钱包，误判成“当下行情已经在交易它”。
4. 不要把 `[高位滞涨方向]` 的回撤，误判成“主线已经结束”。
5. 不要把 `[日线反弹]` 误判成“月线和周线已经重新进入上升趋势”。
6. 不要把 `存储涨价` 直接误判成“整条算力硬件链都一起受益”。
7. 不要把 `超大云厂还在加 CapEx` 误判成“资源运营和算力租赁扩建没有收益率约束”。

---

## 七、本期结论

本期周更的核心，不是告诉我们“哪条线最好”，而是固定当前周度市场结构：

1. `最强主线`：`[待填]`
2. `高位审计区`：`[待填]`
3. `观察接力区`：`[待填]`
4. `个股弹性区`：`[待填]`
5. `修复观察区`：`[待填]`

若 `观察接力区` 是 `AIDC供配电`，建议补写：
`[是否三路径并行 / 谁先验证 / 仍缺什么]`

若 `观察接力区` 是 `AI算力基建大组`，建议补写：
`[四个子层是否共振 / 哪个子层只是旁证 / 是否仍需回到核心供配电三路径]`

如果后面周更里，这五个分层开始变化，就按：

`配置排序 -> 个股验证 -> 行情演绎跟踪表 -> 周更看板 -> dated周更`

这条顺序继续回填，而不是跳过中间台账直接写结论。
